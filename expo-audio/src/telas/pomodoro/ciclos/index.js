import React from "react";
import { View, StyleSheet } from "react-native";
import Ionicons from '@expo/vector-icons/Ionicons';

const renderizaCiclos = (ciclos, subciclos) => {

}

export default function Ciclos({ ciclos, subCiclos }) {
    // for (let count; count < subCiclos; count++) {
    //     return <Ionicons name="square" size={24} color="black" />
    // }

    return (
        <View>
            <Ionicons name="square" size={24} color="black" />
        </View>
    );
}

const styles = StyleSheet.create({

})