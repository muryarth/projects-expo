import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    content: {
        marginBottom: 24,
        flexDirection: "row",
    },

    cicle: {
        flexDirection: "row",
        marginHorizontal: 2,
    }
})

export default styles;