import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    relogio: {
        fontWeight: "bold",
        lineHeight: 100,
        fontSize: 50,
    }
});

export default styles;