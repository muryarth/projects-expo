import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    relogio: {
        fontWeight: "bold",
        lineHeight: 120,
        fontSize: 49,
    }
});

export default styles;